{
  "name": "claimrot",
  "version": "0.1.0",
  "description": "Monitor (claim, source_url) pairs and find out which claims have quietly stopped being true.",
  "license": "MIT",
  "repository": "https://github.com/claimrot/claimrot",
  "type": "module",
  "bin": { "claimrot": "./dist/src/cli.js" },
  "scripts": {
    "build": "tsc && cp src/db/schema.sql dist/src/db/",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "cli": "tsx src/cli.ts"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.70.0",
    "better-sqlite3": "^11.0.0",
    "commander": "^12.0.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.0",
    "@types/node": "^22.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
